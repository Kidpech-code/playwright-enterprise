# Testing Companion Chrome Web Store Submission Guide

This document is a copy-ready checklist for publishing the extension in the Chrome Web Store.

## 1) Prepare Upload Artifact

Run:

```bash
npm run extension:package
```

Expected output file:

```text
release/testing-companion-<version>.zip
```

Current verified file:

```text
release/testing-companion-0.1.0.zip
```

## 2) Draft Listing Fields (Copy-Ready)

### Name

Testing Companion

### Summary (short description)

Manual testing controller for Playwright Enterprise evidence capture and reports.

### Category

Developer Tools

### Language

English

### Detailed Description

Testing Companion helps QA testers, operations teams, and non-developers run guided manual testing sessions with Playwright Enterprise.

The extension acts as a lightweight controller for a local Playwright Agent running on the tester's machine. Testers can start a manual session, pause or resume the flow, capture screenshots, add notes, mark bugs, toggle cursor visibility, and stop the session to generate a local report bundle.

Key capabilities:

- Start, pause, resume, and stop manual testing sessions.
- Capture screenshots and DOM snapshots through the local Playwright Agent.
- Add tester notes, checkpoints, and bug markers.
- Show a clear 3-second guided testing flow: Initialize, Record, Capture, Stop.
- Toggle cursor visibility during manual testing.
- Display the active local workspace path.
- Generate local report artifacts including `summary.html`, screenshots, DOM snapshots, logs, trace, video, and ZIP bundle.

Testing Companion is designed for internal QA workflows. It does not upload test artifacts to a cloud service. Evidence files are written locally by the Playwright Agent to the configured project workspace.

### Support URL

https://github.com/Kidpech-code/playwright-enterprise

### Privacy Policy URL

Host the content from `extension/privacy-policy.md` on a public URL, then place that URL in Chrome Web Store.

## 3) Privacy Disclosure (Data Usage)

Use these answers when filling data safety questions:

- Data sold: No
- Data used for ads: No
- Data shared with third parties: No
- Data collection: Local testing artifacts only, generated by local Playwright Agent
- Data handling location: Local machine in `test-artifacts/session_<timestamp>/`

## 4) Permission Justification (Copy-Ready)

- `activeTab`: Used to send cursor visibility updates to the active browser tab during a manual testing session.
- `tabs`: Used to identify the active tab so the extension can coordinate cursor overlay behavior with the page under test.
- `storage`: Used to remember local extension preferences such as the Playwright Agent WebSocket URL and cursor visibility.
- `<all_urls>`: Required because manual testing may target any local, staging, QA, or production-like environment reachable by the tester's browser.

## 5) Upload Flow

1. Open Chrome Web Store Developer Dashboard.
2. Choose New Item.
3. Upload `release/testing-companion-0.1.0.zip`.
4. Fill listing fields from this file.
5. Add screenshots and icon assets.
6. Add Support URL and Privacy Policy URL.
7. Complete data usage disclosures.
8. Submit for review.

## 6) Final Gate Before Submit

Run:

```bash
npm run extension:prepublish-check
npm run extension:package
```

Confirm:

- `manifest.json` parses successfully
- No blocked code patterns in extension scripts
- Upload zip exists in `release/`
